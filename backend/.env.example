# Environment Variables Configuration
# Copy this file to .env and fill in your actual credentials

# Database Authentication Method
db_auth=sql

# Database Connection Details
db_host=localhost
db_name=db_restoran
db_user=YOUR_DATABASE_USER
db_password=YOUR_DATABASE_PASSWORD
db_trust_server_certificate=true
PORT=3000

# ⚠️ IMPORTANT NOTES:
# - Use SQL Authentication (NOT Windows Auth)
# - Default port 1433 (DO NOT add instanceName to sqlConfig)
# - Make sure Mixed Authentication mode is enabled in SQL Server

# Prisma Database URL
# Format: sqlserver://host:port;database=xxx;user=xxx;password=xxx;encrypt=true;trustServerCertificate=true
DATABASE_URL="sqlserver://localhost:1433;database=db_restoran;user=YOUR_DATABASE_USER;password=YOUR_DATABASE_PASSWORD;encrypt=true;trustServerCertificate=true"

# Shadow database URL (leave empty to disable shadow DB in local dev)
SHADOW_DATABASE_URL=""

# JWT Secret for Authentication
# Generate a secure secret with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET="CHANGE_THIS_TO_A_RANDOM_STRING"

# 📖 SETUP INSTRUCTIONS:
#
# 1. Copy this file:
#    copy .env.example .env
#
# 2. Replace placeholders:
#    - YOUR_DATABASE_USER → your SQL Server username (e.g., prisma_user)
#    - YOUR_DATABASE_PASSWORD → your SQL Server password
#    - JWT_SECRET → generate with the command above
#
# 3. Create SQL Server user if needed:
#    Run: sqlcmd -S localhost -E -i tools/setup-sql-login.sql
#    Or use SSMS to create user manually
#
# 4. Test connection:
#    node test-db.js
#
# 5. Run migrations:
#    npx prisma db push
#    npx prisma generate
#    npm run db:seed
#
# 6. Start server:
#    npm run dev
